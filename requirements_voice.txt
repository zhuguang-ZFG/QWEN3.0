# LiMa device_voice optional dependencies
# Install: pip install -r requirements_voice.txt
#
# These are required for cloud ASR/TTS providers and local voice models.

# Cloud ASR/TTS
alibabacloud-nls-python-sdk>=1.0.2
dashscope>=1.26.0                  # DashScope/Bailian ASR/TTS (uses ALIYUN_API_KEY)
httpx>=0.28.1                    # already in requirements_server.txt
websockets>=16.0                 # already pulled by uvicorn[standard] usually

# Local ASR/VAD/voiceprint (heavy; only needed if using local providers)
edge-tts>=6.1
funasr>=1.1
onnxruntime>=1.16
modelscope>=1.15
torch>=2.0
torchaudio>=2.0
