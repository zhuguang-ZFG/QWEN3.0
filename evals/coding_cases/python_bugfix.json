{
  "id": "python_bugfix",
  "name": "Fix a Python off-by-one bug",
  "prompt": "Fix this Python function. Return only the corrected function in a Python code block.\\n\\n```python\\ndef last_item(items):\\n    return items[len(items)]\\n```",
  "required_patterns": ["def last_item", "items\\[-1\\]|items\\[len\\(items\\) - 1\\]"],
  "forbidden_patterns": ["I cannot", "as an AI", "IndexError"],
  "min_chars": 35,
  "max_tokens": 256,
  "python_must_compile": true,
  "tags": ["python", "bugfix"]
}
