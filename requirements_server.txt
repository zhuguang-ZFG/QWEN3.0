# LiMa Router server deps (Python 3.10+)
# Install: pip install -r requirements_server.txt
#
# AUDIT-7-D4: 依赖版本收紧。原全部用 >=（无上限），任何破坏性新版本会被自动拉入
# （项目已遭遇 fastapi 0.136.3 MAL-2026-4750 恶意版本事件）。现改为 ~=（锁定主.次版本，
# 允许补丁更新）或 ==（精确锁定），兼顾安全补丁与构建可复现性。

# Exclude fastapi 0.136.3 (MAL-2026-4750 malicious release on PyPI)
fastapi>=0.115.0,<0.136.3
uvicorn[standard]~=0.48.0
httpx~=0.28.0
python-dotenv~=1.2.2
paramiko~=5.0.0
numpy>=2.2.6,<2.3
redis~=8.0.1
python-multipart>=0.0.9,<1.0

# Dev / CI (test runner, type checking, dead-dep scanning)
# Moved to requirements_dev.txt
# hypothesis>=6.0
# pyright>=1.1.0
# deptry>=0.20
# pytest-timeout>=2.0

# Runtime metrics export
prometheus_client~=0.25.0

# Optional: MQTT device transport
paho-mqtt~=2.1.0

# XiaoZhi compat
PyJWT~=2.13.0
bcrypt~=5.0.0

# 绘画引擎依赖（战略转型新增 2026-06-09）
opencv-python-headless==4.10.0.84  # 轮廓检测（无 GUI，适合服务器）
scikit-image>=0.22.0,<0.25         # 骨架化（Zhang-Suen，笔绘机单线路径）
dashscope==1.20.11         # 通义千问 SDK（文生图）
Pillow==10.4.0             # 图像处理
fonttools~=4.63.0          # 手写体字形提取（文字转 SVG 路径，写字机）

# Optional: Instructor-based structured outputs (P4-3)
# instructor>=1.0.0
