# DLC Drawing Service deps (Python 3.10+)
# Install: pip install -r requirements_server.txt
#
# P5 瘦身后：旧 Chat/Routing/Admin/Voice 依赖已随代码删除。
# 保留依赖均为 dlc_api / dlc_core / dlc_mcp / device_gateway 生产路径使用。

# Web 框架
fastapi>=0.136.1,<0.136.3   # 排除 MAL-2026-4750 恶意版本
starlette>=1.3.1             # transitive via fastapi; CVE-2026-54282/54283
uvicorn[standard]~=0.50.0

# HTTP 客户端（设备通信 + 小程序 HTTP 路径调用 dlc_api）
httpx~=0.28.0

# 配置加载
python-dotenv~=1.2.2

# 部署工具（deploy_unified.py SSH 上传）
paramiko~=5.0.0
cryptography>=49.0.0         # transitive via paramiko

# 数据层
redis~=8.0.1                 # 设备任务队列（RedisDeviceTaskStore）
python-multipart>=0.0.32,<1.0  # FastAPI 文件上传（图库）

# 运维指标（observability/prometheus_metrics.py）
prometheus_client~=0.25.0

# MQTT 设备通信
paho-mqtt~=2.1.0

# 设备鉴权（device_logic/auth.py JWT + bcrypt）
PyJWT~=2.13.0
bcrypt~=5.0.0

# 绘画引擎（dlc_core 路径生成 + 图像矢量化）
numpy>=1.26.0,<2.3
opencv-python-headless==4.10.0.84  # 轮廓检测（无 GUI，适合服务器）
scikit-image>=0.22.0,<0.25         # 骨架化（Zhang-Suen，笔绘机单线路径）
dashscope==1.26.3                 # 通义千问 SDK（文生图，小程序 HTTP 路径保留）
Pillow~=12.3.0                     # 图像处理
fonttools~=4.63.0                  # 手写体字形提取（文字转 SVG 路径，写字机）
