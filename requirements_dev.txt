# LiMa development/test/lint dependencies
# Install: pip install -r requirements_dev.txt
#
# AUDIT-7-D4: 版本收紧（~= 锁定主.次，允许补丁更新）

# Test and static analysis tools (moved from requirements_server.txt)
hypothesis~=6.155
pyright>=1.1.411
deptry~=0.20
pytest-timeout~=2.0
freezegun~=1.5
httpx2~=2.5  # starlette.testclient prefers httpx2 over httpx; suppresses deprecation warning
