# LiMa — lima.257339.xyz (legacy domain → chat.donglicao.com)
# Deploy: cp to /etc/nginx/conf.d/lima.257339.xyz.conf && nginx -t && systemctl reload nginx

server {
    listen 80;
    server_name lima.257339.xyz;
    return 301 https://chat.donglicao.com$request_uri;
}
