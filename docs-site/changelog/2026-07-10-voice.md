# 2026-07-10 小程序语音栈加固

## 后端

- 恢复并加固设备 App 语音 REST/WS（`device_voice/`、`routes/device_app_voice*.py`）。
- REST：`POST /device/v1/app/voice/transcribe`（ASR + `resolve_voice_task`）。
- WS：`/device/v1/app/voice/ws` 与 `/v1/voice` 兼容别名（ASR-only，非旧 LLM/TTS 管道）。
- 最短 PCM 校验（默认 3200 字节）、流式分帧 pacing（1280B / 40ms）、`ping`/`pong` 保活。
- ASR provider：DashScope（默认）、FunASR、Whisper；legacy `aliyun_fallback` 映射至 dashscope。

## 部署与验证

- 默认生产部署目标：京东云 `117.72.118.95`（`deploy_unified.py --target jdcloud`）。
- nginx：`/v1/voice` → `server_dlc` :8081。
- 生产 strict E2E：`LIMA_VOICE_E2E_STRICT=1 python scripts/run_voice_e2e_production.py` → 6/6 PASS。

## 文档

- 修正 `docs-site/api/voice.md`：ticket `expires_in=30`；WS 不返回 `intent`
- 新增 `docs/PROJECT_STATUS_CN.md`（与 `STATUS.md` 同步）；修正部署文档中的 `dlc-drawing` / `/opt/dlc-drawing/backups/`

## 待办

- 真机全链路验证（P0-3）、微信审核（P0-4）、U8 OPUS/PCM（P0-2）。
