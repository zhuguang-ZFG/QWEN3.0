{
  "mcpServers": {
    "lima-mimo": {
      "command": "python",
      "args": ["-m", "lima_mcp_stdio"],
      "env": {
        "MIMO_MCP_WORKSPACE": "${workspaceFolder}",
        "LIMA_TIMEOUT": "300",
        "MIMO_MCP_SKIP_PERMISSIONS": "1"
      }
    }
  }
}
